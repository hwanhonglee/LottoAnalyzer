# Lotto Analyzer V9 보안 설계

## 이번 버전에 실제 반영된 통제

- 화면 서버는 `127.0.0.1`에만 바인딩하며 외부 LAN/인터넷에 공개하지 않습니다.
- 최신 데이터는 고정된 `raw.githubusercontent.com` HTTPS URL 두 개만 허용합니다.
- TLS 인증서/호스트명을 검증하고 TLS 1.2 이상을 요구합니다.
- 응답 크기를 제한하고 UTF-8 JSON만 허용합니다.
- 회차, 날짜, 번호 범위, 번호 중복, 보너스번호, 전체 회차 연속성을 검증합니다.
- 업데이트 직전에 SQLite Backup API로 안전 백업을 만들고 최근 5개를 유지합니다.
- 업데이트는 트랜잭션으로 처리하며 `PRAGMA quick_check`와 회차 연속성을 확인합니다.
- DB는 `%LOCALAPPDATA%\LottoAnalyzer`에 두어 Program Files 쓰기 권한 문제를 피합니다.
- Streamlit 사용 통계 전송을 비활성화하고 계정/개인정보를 수집하지 않습니다.
- 배포 폴더의 실행 파일/라이브러리에 대한 SHA-256 무결성 확인 스크립트를 제공합니다.

## 배포 전에 추가로 해야 하는 것

`LottoAnalyzer.exe`와 설치 프로그램을 개발자 본인 명의의 Windows 코드서명 인증서로 Authenticode 서명해야 합니다. 인증서 개인키는 타인에게 공유하거나 프로그램 소스에 포함하면 안 됩니다. Microsoft Store/MSIX 배포도 대안입니다.

## 제한

공개 JSON 미러는 동기화 편의를 위한 보조 소스입니다. 엄격한 형식·연속성 검증은 악성/깨진 입력 위험을 낮추지만, 공개 미러의 내용이 공식 추첨 결과와 완전히 동일하다는 암호학적 보증은 아닙니다. 중요한 확인은 동행복권 공식 결과를 기준으로 해야 합니다.

## 보안 취약점 및 오류 제보

- 개발자: **이환홍**
- 이메일: **hwanhong57@gmail.com**

제보 시 실제 비밀번호, 인증서 개인키, GitHub 토큰 등의 비밀정보는 첨부하지 마세요.
